# README

Modules:
- pyautogui

Compiler:
- MINGW (added bin to Environment PATH) for C++ (g++ CMD)
- Code Runner, run with CTRL+ALT+N

Compiler checker:
- C# = dotnet --version
- Python = python --version
- C++ = g++ --version